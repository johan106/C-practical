#include <stdio.h>
#include <math.h>
int main(){
    float a,b,c,discriminant,root1,root2;
        printf("Enter the 3 coefficient:");
        scanf("%f %f %f",&a,&b,&c);
        discriminant=(b*b)-(4*a*c);
        if (discriminant>0)
        {
           
        }
        
    return 0;
}